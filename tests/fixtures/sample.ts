// Sample TypeScript file for testing
const x = 1;
let y = 'hello'; // Formatting issues
function unused() {} // Lint issue
const z: string = 123; // Type error

export function testFunction(param: number): string {
  return param.toString();
}
